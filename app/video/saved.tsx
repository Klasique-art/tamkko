import React from 'react';

import { ScreenScaffold } from '@/components/scaffold';

export default function SavedVideosScreen() {
    return (
        <ScreenScaffold
            title="Saved Videos"
            subtitle="Bookmarked videos list with quick replay and unsave actions."
        />
    );
}
